version: '3.8'

services:
  app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - STREAMLIT_SERVER_PORT=8501
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
      - PYTHONUNBUFFERED=1
    # Mantém o container rodando e atualiza ao salvar arquivos no host
    command: streamlit run app.py --server.port=8501 --server.address=0.0.0.0
